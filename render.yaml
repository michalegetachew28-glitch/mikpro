services:
  # Express Backend API
  - type: web
    name: garage-backend
    env: node
    rootDir: backend
    plan: free
    buildCommand: npm install && npx prisma generate
    startCommand: npx prisma db push && node index.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: garage-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          name: garage-frontend
          property: host

  # Vite React Frontend Static Site
  - type: static
    name: garage-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://garage-backend.onrender.com/api # Replace garage-backend with your actual backend service name on Render
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: garage-db
    plan: free
